<?php
/** @var Sebaks\View\ViewModel $viewModel */
$viewModel = $this->viewModel()->getCurrent();
$children = $viewModel->getChildren();
?>

<tr>
    <?php
    /** @var Sebaks\View\ViewModel $child */
    foreach ($children as $child) {
        echo $this->render($child);
    }
    ?>
    <th>Actions</th>
</tr>
